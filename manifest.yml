modules:
  jira:dashboardGadget:
    - key: devops-request-type-gadget
      title: Work by Request Type
      description: >-
        Issue count broken down by JSM request type, grouped dynamically from
        customfield_10010 (no hardcoded catalog), sourced from a Jira filter.
      thumbnail: https://developer.atlassian.com/platform/forge/images/icons/issue-panel-icon.svg
      resource: main-resource
      render: native
      resolver:
        function: resolver
      edit:
        resource: main-resource
        render: native
  function:
    - key: resolver
      handler: index.handler
resources:
  - key: main-resource
    path: src/frontend/index.jsx
permissions:
  scopes:
    - read:jira-work
app:
  runtime:
    name: nodejs24.x
    memoryMB: 256
    architecture: arm64
  # The real app id is injected from the APP_ID env var at deploy time (see
  # .env.example / scripts/forge-env.sh). Keeps the repo free of a hardcoded id.
  id: ${APP_ID}
environment:
  variables:
    - key: APP_ID
      default: ari:cloud:ecosystem::app/00000000-0000-0000-0000-000000000000
